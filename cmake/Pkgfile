# Description:	A cross-platform, open-source make system.
# URL:		http://www.cmake.org/
# Maintainer:	Fredrik Rinnestam, fredrik at crux dot nu
# Depends on:	curl expat jsoncpp libarchive libuv ncurses zstd
# Modified:	Milan Buska, milan dot buska at gmail dot com

name=cmake
version=3.18.0
release=1
source=(http://www.cmake.org/files/v${version%.*}/$name-$version.tar.gz)

build() {
	cd $name-$version

	sed -i Source/CMakeInstallDestinations.cmake \
	-e 's|share/cmake-${CMake_VERSION_MAJOR}.${CMake_VERSION_MINOR}|share/cmake|'
	sed -i '/"lib64"/s/64//' Modules/GNUInstallDirs.cmake

	./bootstrap \
		--prefix=/usr \
		--system-libs \
		--no-system-librhash

	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/doc
	ln -sf $name $PKG/usr/share/$name-${version%.*}
}
