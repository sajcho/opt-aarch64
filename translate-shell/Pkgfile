# Description:	A command-line interface and interactive shell for Google Translate 
# URL:		https://www.soimort.org/translate-shell/
# Maintainer:	sajcho, saux dot aarch64 at gmail dot com
# Depends on:	bash curl fribidi gawk less rlwrap

name=translate-shell
version=0.9.6.12
release=0
source=(https://github.com/soimort/${name}/archive/v${version}.tar.gz)

build() {
	cd ${name}-${version}

	make

	make \
		PREFIX=/usr \
		DESTDIR=$PKG install
}
