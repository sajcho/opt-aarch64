# Description:	Utility library for managing the libnvdimm (non-volatile memory device) sub-system in the Linux kernel
# URL:		https://github.com/pmem/ndctl
# Maintainer:	sajcho, saux dot aarch64 at gmail dot com
# Depends on:	autoconf automake eudev json-c keyutils kmod libtool util-linux

name=ndctl
version=71.1
release=0
source=(https://github.com/pmem/${name}/archive/v${version}/${name}-${version}.tar.gz)

build() {
	cd ${name}-${version}

	./autogen.sh

	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--bindir=/usr/sbin \
		--disable-asciidoctor \
		--disable-docs \
		--without-systemd \
		--without-bash

	make

	make DESTDIR=$PKG install
}
