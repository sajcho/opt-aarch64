
Note:

    Use Harfbuzz library to improve auto-hinting
    if available, many glyphs not directly addressable
    by a fonts character map will be hinted also.

Freetype recommended:

    First, install without harfbuzz, after it is installed harfbuzz,
    reinstall freetype with harfbuzz.


# Subpixel hinting mode can be chosen by setting the right TrueType interpreter
# version. The available settings are:
#
#     truetype:interpreter-version=35  # Classic mode (default in 2.6)
#     truetype:interpreter-version=38  # Infinality mode
#     truetype:interpreter-version=40  # Minimal mode (default in 2.7)
#
# There are more properties that can be set, separated by whitespace. Please
# refer to the FreeType documentation for details.

# add the line below to your /etc/profile ( or .profile,.bash_profile)
export FREETYPE_PROPERTIES="truetype:interpreter-version=40"