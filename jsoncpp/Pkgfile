# Description:	A JSON implementation in C++
# URL:		https://github.com/open-source-parsers/jsoncpp
# Maintainer:	Natanael Copa, ncopa at alpinelinux dot org
# Depends on:	meson
# Modified:	Milan Buska, milan dot buska at gmail dot com

name=jsoncpp
version=1.9.2
release=1
source=(https://github.com/open-source-parsers/$name/archive/$version.tar.gz)

build() {
	cd $name-$version

	meson \
		--prefix=/usr \
		--buildtype=plain \
		--libdir=/usr/lib \
		--default-library=both \
		build

	ninja -C build
	DESTDIR=$PKG ninja -C build install
}
