# Description:	GLib bindings for DBUS - use dbus-elogind
# URL:		http://www.freedesktop.org/wiki/Software/DBusBindings
# Maintainer:	sajcho, saux dot aarch64 at gmail dot com
# Depends on:	dbus-elogind glib

name=dbus-glib-elogd
_realname=dbus-glib
version=0.112
release=0
source=(https://dbus.freedesktop.org/releases/${_realname}/${_realname}-${version}.tar.gz)

build() {
	cd ${_realname}-${version}

	./configure \
		--prefix=/usr \
		--disable-tests \
		--disable-static \
		--disable-bash-completion

	make

	make DESTDIR=$PKG install

	rm -r $PKG/usr/share/gtk-doc
}
